---
galaxy_info:
  author: {{ cookiecutter.author }}
  description: {{ cookiecutter.description }}
  company: {{ cookiecutter.company }}

  # If the issue tracker for your role is not on github, uncomment the
  # next line and provide a value
  # issue_tracker_url: http://example.com/issue/tracker

  license: {{ cookiecutter.license }}
  min_ansible_version: {{ cookiecutter.min_ansible_version }}

  # If this a Container Enabled role, provide the minimum Ansible Container version.
  # min_ansible_container_version:

  # Optionally specify the branch Galaxy will use when accessing the GitHub
  # repo for this role. During role install, if no tags are available,
  # Galaxy will use this branch. During import Galaxy will access files on
  # this branch. If Travis integration is configured, only notifications for this
  # branch will be accepted. Otherwise, in all cases, the repo's default branch
  # (usually master) will be used.
  # github_branch:

  platforms:
{% if cookiecutter.platform_alpine_versions %}
  - name: Alpine
    versions:
{% for version in cookiecutter.platform_alpine_versions %}
    - {{ version }}
{% endfor %}
{% endif %}
{% if cookiecutter.platform_redhat_versions %}
  - name: EL
    versions:
{% for version in cookiecutter.platform_redhat_versions %}
    - {{ version }}
{% endfor %}
{% endif %}
{% if cookiecutter.platform_fedora_versions %}
  - name: Fedora
    versions:
{% for version in cookiecutter.platform_fedora_versions %}
    - {{ version }}
{% endfor %}
{% endif %}
{% if cookiecutter.platform_debian_versions %}
  - name: Debian
    versions:
{% for version in cookiecutter.platform_debian_versions %}
    - {{ version }}
{% endfor %}
{% endif %}
{% if cookiecutter.platform_ubuntu_versions %}
  - name: Ubuntu
    versions:
{% for version in cookiecutter.platform_ubuntu_versions %}
    - {{ version }}
{% endfor %}
{% endif %}
{% if cookiecutter.categories %}
  categories:
{% for cat in cookiecutter.categories %}
    - {{ cat }}
{% endfor %}
{% endif %}
{% if cookiecutter.galaxy_tags %}
  galaxy_tags:
{% for tag in cookiecutter.galaxy_tags %}
    - {{ tag }}
{% endfor %}
{% else %}
  galaxy_tags: []
  # List tags for your role here, one per line. A tag is a keyword that describes
  # and categorizes the role. Users find roles by searching for tags. Be sure to
  # remove the '[]' above, if you add tags to this list.
  #
  # NOTE: A tag is limited to a single word comprised of alphanumeric characters.
  #       Maximum 20 tags per role.
{% endif %}

{% if cookiecutter.dependencies %}
dependencies:
{% for dependency in cookiecutter.dependencies %}
  - {{ dependency }}
{% endfor %}
{% else %}
dependencies: []
# List your role dependencies here, one per line. Be sure to remove the '[]' above,
# if you add dependencies to this list.
{% endif %}